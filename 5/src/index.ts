import getInput from '../../shared/getInput';
import calculateNumberOfOverlaps from './calculateNumberOfOverlaps';

const input = getInput();

console.log(`Number of overlaps: ${calculateNumberOfOverlaps(input)}`);
